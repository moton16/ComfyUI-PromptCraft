__pycache__/
docs/
.mimocode/
.gstack/
CODE_QUALITY_FIX_PLAN.md
.claude/
node_modules/
.ruff_cache/
.pytest_cache/
*.pyc
*.pyo
.env
*.log

# Node.js
node_modules/
package-lock.json
CLAUDE.md

# Vite 构建产物：有意纳入 git 追踪。
# ComfyUI Manager / Registry / git clone 等分发渠道均无构建步骤，
# 必须随仓库提交编译后的产物，否则用户端 Vue 弹窗不可用。
# 发布前必须重新执行 npm run build 并提交更新。

# 本地草稿与临时工作目录（不参与构建/发布）
work/

# 本地 release 打包暂存目录（本地测试用，不应提交）
staging/

# pack_release.py 打包产物目录（本地测试用，不应提交）
dist/

# 本地用户配置数据（运行时生成，不应提交）
data/lora_groups.json
data/lora_prompts.json
data/lora_favorites.json
data/prompt_history.json

# V0-SEC-01: 备份文件与运行时缓存（可能含用户数据）
*.bak
*.prompt.bak
*.pc-info.json

# V0-SEC-01: 含 API key 的本地配置（用户配置目录运行时生成）
user_config/
data/llm_config.json
data/llm_services.json
data/llm_system_prompt.json
data/custom_thinking_rules.json
data/custom_thinking_params.json

