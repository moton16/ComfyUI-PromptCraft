# PromptCraft v1.2.1 Mod3 开发总结

> 日期: 2026-05-25
> 版本跨度: V1.2.1 Mod2 → V1.2.1 Mod3

---

## 一、本次优化内容

### 优化 1：修复浮窗 + 右键关闭 + 设置开关

**问题**：浮动快捷面板（◆ 按钮）可能因 localStorage 标记未正确初始化而不显示。

**修复**：
- `js/index.js` 的 `ensureFallbackPanel()` 函数开头增加 `localStorage.getItem('moton-pe-panel-hidden')` 检查
- 添加右键上下文菜单，包含"关闭浮窗"选项
- 关闭后弹窗提示"可通过 PromptCraft 设置面板重新开启"
- `js/control_panel.js` 新增 `buildPanelSwitch()` 浮窗开关区块，嵌入设置面板
- 新增 `toggleFallbackPanel(visible)` 全局函数和 `promptcraft:toggle-panel` 事件监听

**效果**：浮窗正常显示，右键可关闭，设置面板可控制开关。

---

### 优化 2：大模型扩写模式分开设置 API 接入（3 种）

**问题**：基础扩写和详细扩写共用同一个 API 服务配置，无法独立设置。

**方案**：
- `data/llm_services.json` 的 `current` 字段从 `enhance`/`agent` 扩展为 `enhance_basic`/`enhance_detail`/`enhance_normal`/`agent`
- `config_manager.py` 更新 `_get_default_services_config()` 和 `set_current_service()` 支持 4 个类别
- 添加迁移逻辑：旧版 `enhance` 自动迁移到 `enhance_basic`
- `prompt_enhancer.py` 的 `扩写模式` 从 `["基础扩写", "详细扩写"]` 改为 `["基础扩写", "详细扩写", "普通扩写"]`
- `_get_llm_client()` 方法接受 mode 参数，按模式映射到对应 category
- `js/control_panel.js` 的 API 服务配置区块扩展为 4 个分类下拉选择器

**效果**：三种扩写模式可独立配置不同的 API 服务和模型。

---

### 优化 3：浮窗查看历史 Prompt 功能

**问题**：浮动快捷面板中缺少 Prompt 历史入口。

**方案**：
- `js/index.js` 的 `QUICK_BUTTONS` 数组添加 `{ text: '⏱ Prompt 历史', fn: openPromptHistory }`

**效果**：浮窗菜单新增"Prompt 历史"按钮，点击可查看和复用历史提示词。

---

### 优化 4：负面提示词移除独立管理，加入双库范围

**问题**：负面提示词通过独立的编辑器管理，与双库体系分离。

**方案**：
- `data/sfw_prompts.json`：移除顶层 `negative_prompts` 字典，在 `categories` 中 `质量等级` 之后新增 `负面提示词` 分类（标准/高质量/简单）
- `data/nsfw_prompts.json`：新增 `负面提示词` 分类
- `prompt_enhancer.py`：`INPUT_TYPES` 中 `负面提示词类型` 改用 `_build_category_full_options("负面提示词")`，`_generate_negative()` 改为从双库 categories 查找
- `js/control_panel.js`：移除"负面提示词"工具卡片和事件处理
- `js/index.js`：浮窗 QUICK_BUTTONS 移除"负面 Prompt"，移除事件监听

**效果**：负面提示词通过双库管理，可在 Prompt 库编辑器中统一编辑。

---

### 优化 5：节点字段改名

**改名对照**：

| 旧名 | 新名 |
|------|------|
| `服饰` | `服饰细节` |
| `情绪氛围` | `表情状态` |
| `权重_情绪` | `权重_状态` |

**修改文件**：
- `prompt_enhancer.py`：INPUT_TYPES、IS_CHANGED category_keys、generate() 中所有引用
- `data/sfw_prompts.json`：categories key、presets 中所有引用
- `data/nsfw_prompts.json`：categories key
- `js/index.js`：CATEGORY_COMBO_NAMES 数组

---

### 优化 6：移除/新增节点字段

**移除**：
- `角色类型`：从 INPUT_TYPES、trigger_words、generate() 逻辑中移除
- `光源类型`：从 INPUT_TYPES、categories、generate() lighting_categories 中移除

**新增**：
- `时间设定`：清晨/上午/午后/黄昏/傍晚/夜间/深夜/雨天/雪天/阴天（带背景描述 en tag）
- `情绪表达(忌与表情状态同时随机)`：平淡的/开心的/忧郁的/愤怒的/害羞的/慵懒的/兴奋的/冷漠的/沉思的/惊讶的/恐惧的/陶醉的

**修改文件**：
- `prompt_enhancer.py`：INPUT_TYPES、IS_CHANGED、generate()
- `data/sfw_prompts.json`：categories 新增、trigger_words 移除角色类型
- `data/nsfw_prompts.json`：categories 新增/移除
- `js/index.js`：CATEGORY_COMBO_NAMES 更新

---

## 二、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `prompt_enhancer.py` | 修改 | 字段改名、移除/新增字段、负面提示词重构、扩写模式3分类 |
| `config_manager.py` | 修改 | 4类别服务配置、迁移逻辑、VALID_CATEGORIES 更新 |
| `data/sfw_prompts.json` | 修改 | categories key 改名、移除/新增分类、negative_prompts 移除 |
| `data/nsfw_prompts.json` | 修改 | categories key 改名、移除/新增分类 |
| `data/llm_services.json` | 修改 | current 扩展为 4 类别、版本升级 3.0.0 |
| `js/index.js` | 修改 | CATEGORY_COMBO_NAMES、浮窗右键菜单、开关函数、事件监听 |
| `js/control_panel.js` | 修改 | 4分类下拉、浮窗开关区块、移除负面提示词卡片 |
| `__init__.py` | 修改 | 版本号更新 |

---

## 三、技术细节备忘

### 服务配置迁移逻辑

`config_manager.py` 的 `load_services_config()` 中添加了旧版迁移逻辑：
- 检测 `current` 中存在 `enhance` 但不存在 `enhance_basic`
- 自动将 `enhance` 的值迁移到 `enhance_basic`
- 同时创建 `enhance_detail` 和 `enhance_normal`（复用 enhance 的 service_id）

### 负面提示词双库查找

`_generate_negative()` 改为从双库的 `categories.负面提示词` 中查找：
- 先查 SFW 库，再查 NSFW 库
- 按 label 精确匹配
- 兜底返回 `"low quality, worst quality, normal quality"`

### 情绪表达字段名

画布节点上显示为 `情绪表达(忌与表情状态同时随机)`，使用完整字段名作为 INPUT_TYPES key，generate() 中 kwargs.get 使用同一名称。

---

## 四、后续可优化方向

1. **双库负面提示词扩展**：NSFW 库的负面提示词目前只有占位数据，可补充 NSFW 特有的负面标签
2. **时间设定/情绪表达的 NSFW 库数据**：当前只有占位示例，可补充实际数据
3. **服务配置 UI 优化**：4 个分类下拉选择器可能在窄屏下换行，可考虑响应式布局
4. **浮窗位置记忆**：浮窗位置已通过 localStorage 持久化，可考虑添加重置位置功能
